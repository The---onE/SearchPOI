
<!--pages/map/map.wxml-->
<view class="board">
  <map class="map" longitude="{{position.longitude}}" latitude="{{position.latitude}}" scale="{{scala}}" markers="{{markers}}" bindmarkertap="onMarkerTap" />
</view>